#include <iostream>
using namespace std;
#pragma once

class SimpleCalculator
{
public:
	float Add(float numA, float numB);
	float Subtract(float numA, float numB);
	float Multiply(float numA, float numB);
	float Divide(float numA, float numB);
};